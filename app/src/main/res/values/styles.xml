<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--verificar esto en el emultador-->
        <item name="android:windowSoftInputMode">stateHidden</item>

    </style>

    <!-- Base application theme. -->
    <!--<style name="AppTheme.Dialog" parent="Base.Theme.AppCompat.Light.Dialog.MinWidth">-->
    <style name="AppTheme.Dialog" parent="Base.Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Base application - used to the activities that en w900 are showed as . -->
    <style name="AppTheme.Dialog_NoActionBar" parent="AppTheme.NoActionBar" />


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
